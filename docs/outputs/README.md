# SWAT+ Output Reference Pages

Detailed output-file pages will live here once writer routines, units rows, output frequencies, object indexing, and reset timing are traced.

This folder intentionally has no committed category subfolders yet. Add a category only with the first evidence-backed output page in that category, so the output reference does not imply traced coverage that does not exist.

Start with [`../output-files.md`](../output-files.md) for the current output map.
